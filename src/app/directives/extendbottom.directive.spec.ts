import { ExtendbottomDirective } from './extendbottom.directive';

describe('ExtendbottomDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtendbottomDirective();
    expect(directive).toBeTruthy();
  });
});
