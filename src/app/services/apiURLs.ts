export const serverURL = `http://dev-office.ddns.net/Solutions/api`;


export const AUTH_ENs = {
    login: `${serverURL}/auth/Login`,
}

export const ROLES_ENs = {
    getSystemRoles: `${serverURL}/Identity/Role/GetAll`,
    getMenuByRoleId: `${serverURL}/MenuList/GetMenuByRoleId`
}

